import React, { Component } from 'react';
//import { connect } from 'react-redux';

const SearchHeader = () => {
    return (
        <form  className="search-header">
              Search : <input className="" placeholder="Search"  />
         </form> 
    );
};

export default SearchHeader;